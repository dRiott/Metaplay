/**
 * 
 */

$(document).ready(function () {
	
	for(var i = $("#artistsTable tr").length; i-=1;) {
		var j = i;
		$("#showButton"+i).click(function () {
			console.log("Button id: " + $(this).attr('id'));
			console.log("Looking for the span's id: " + $(this).closest('td').find('span').attr('id'));
			console.log("j: " + j);
			var buttonId = "#moreLessBiography"+i;
			console.log(buttonId);
			console.log($(buttonId).attr('id'));
		})
	}
	
	
//	$("tr").each(function() {
////		  $this = $(this)
////		  var value = $this.find("span.value").html();
////		  var quantity = $this.find("input.quantity").val();
//		  console.log("GET");
//		});
//	
	
}); //end document.ready

